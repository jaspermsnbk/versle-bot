{
  "name": "versle-bot",
  "version": "1.0.0",
  "main": "app.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && nodemon -e py --exec 'flask --app app run' --config nodemon.json",
    "build": "python -m venv .venv && bash -c 'source .venv/bin/activate' && python -m pip install -r requirements.txt"
  },
  "author": "jasper mesenbrink",
  "license": "ISC",
  "description": "",
  "repository": {
    "url": "git@github.com:jaspermsnbk/versle-bot.git"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
